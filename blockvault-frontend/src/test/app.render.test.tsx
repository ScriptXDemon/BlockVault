// Skipped: Cannot run due to import.meta.env usage in VaultUpload
test.skip('renders heading', () => {
  // render(<App />);
  // expect(screen.getByText(/BlockVault Phase 1/i)).toBeInTheDocument();
});
